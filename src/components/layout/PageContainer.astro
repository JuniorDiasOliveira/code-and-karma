---
export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`min-h-screen flex flex-col bg-background text-foreground ${className}`}>
  <slot />
</div>